# Optional. Without this the contact form still works: it logs the message
# server-side and returns success. Add the key to receive real emails.
RESEND_API_KEY=
CONTACT_TO_EMAIL=syedasharabhassanshah@gmail.com
